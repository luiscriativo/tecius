// Re-exporta tipos do projeto base (mantidos para compatibilidade)
export type { Theme, Notification, NotificationType, NavItem, ApiResult, Platform } from './legacy'

// Exporta tipos do dominio Chronicler
export * from './chronicler'
